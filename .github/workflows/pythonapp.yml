name: CI

on: [push]

jobs:
  build_ubuntu:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: [3.7]
    steps:
    - uses: actions/checkout@v1
    - name: Set up Python ${{ matrix.python }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python }}
    - name: install apt packages
      run: |
        sudo apt-get update
        sudo apt-get install postgresql-12 postgresql-client-12 openimageio-tools
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build --info
    - name: Lint with Gradle
      run: ./gradlew lint --info

  build_windows:
    runs-on: windows-latest
    strategy:
      matrix:
        python: [ 3.7 ]
    steps:
      - uses: actions/checkout@v1
      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python }}
      - name: Setup OpenImageIO
        run: |
          curl --location https://github.com/Latios96/oiio-building/releases/download/v2.2.7.0-test/openimageio-2.2.7.0-windows.tar > openimageio-2.2.7.0-windows.tar
          tar -xvf openimageio-2.2.7.0-windows.tar
          echo "{./build/bin}" >> $GITHUB_PATH
      - name: Build with Gradle
        run: .\gradlew build --info
      - name: Lint with Gradle
        run: .\gradlew lint --info
